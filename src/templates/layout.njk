{% set meta = {
    url: 'http://simthesocialrobot.com',
    title: 'SIM: The Social Robot',
    desc: 'Designing for Affective Human-Robot Interaction',
    site: 'SIM: The Social Robot',
    image: '/img/sim_badge.png'
}%}

<!DOCTYPE html>
<html lang="en">
    <head>
        {# Metadata #}
        {% include "partials/meta.njk" %}

        {# JavaScript #}
        <script src="/scripts/jquery-3.2.1.min.js"></script>
        <script>
        // https://stackoverflow.com/a/29913175/705263
        window.getDevicePixelRatio = function () {
            var ratio = 1;
            // To account for zoom, change to use deviceXDPI instead of systemXDPI
            if (window.screen.systemXDPI !== undefined && window.screen.logicalXDPI       !== undefined && window.screen.systemXDPI > window.screen.logicalXDPI) {
                // Only allow for values > 1
                ratio = window.screen.systemXDPI / window.screen.logicalXDPI;
            }
            else if (window.devicePixelRatio !== undefined) {
                ratio = window.devicePixelRatio;
            }
            return ratio;
        };
        </script>

        {# Google Fonts #}

        {# Stylesheets #}
        <link rel="stylesheet" type="text/css" href="css/style.css" />
        {% include "partials/analytics.njk" %}
    </head>

    <body class="{{ var.bodyClass }} ">
        {% block content %}{% endblock %}
    </body>
</html>
